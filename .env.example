# Copy to .env and adjust for your machine. Never commit the real .env.
# These values are local development defaults only — not production credentials.

POSTGRES_DB=aicompany
POSTGRES_USER=aicompany
POSTGRES_PASSWORD=aicompany_local_dev
POSTGRES_PORT=5432
POSTGRES_HOST=localhost
