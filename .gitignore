# Local environment — never commit real credentials
.env
.env.*
# ...except the template, which exists to be committed (review R7 of TASK-001:
# .env.* was not covered, so .env.production would have been tracked).
!.env.example

# Build output
target/
build/
dist/
out/

# Local database data (should live in a Docker volume, not in the repo)
pgdata/
postgres-data/

# IDE
.idea/
.vscode/
*.iml
*.iws
*.ipr

# OS
.DS_Store
Thumbs.db
