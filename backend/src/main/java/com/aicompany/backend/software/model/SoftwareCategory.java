package com.aicompany.backend.software.model;

/** Closed vocabulary of {@code software.category}, mirrored by {@code software_category_check} (V12). */
public enum SoftwareCategory {
    AGENTIC_IDE, IDE, EDITOR, TERMINAL, AI_CLOUD, AI_LOCAL, DATABASE, API,
    INFRASTRUCTURE, DIAGRAM, DESIGN, THREE_D, VCS, HOSTING, PRODUCTIVITY
}
