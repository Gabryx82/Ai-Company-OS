package com.aicompany.backend.project.model;

/** What kind of software a project builds. {@code projects_project_type_check} (V14). */
public enum ProjectType {
    WEB_APP, BACKEND, MOBILE, DESKTOP, AI_ML, GAME, THREE_D, DATA, AUTOMATION, API, FULL_STACK, OTHER
}
