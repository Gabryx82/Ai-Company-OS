# Tests run against a throwaway PostgreSQL container provided by Testcontainers.
# Only schema migrations are applied here: test data is created by the tests
# themselves, so the development seed cannot make assertions pass by accident.
spring.flyway.locations=classpath:db/migration
