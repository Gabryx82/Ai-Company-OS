# TASK-021 — Artefatto

Vedi `TASK.md` §Artefatto (file, test, mutazioni). Nessuna migrazione; nessuna rottura di contratto
(tutto additivo: una rotta nuova, un `type` nuovo).

Debito: il rilievo L-3 di TASK-011 («l'elenco degli endpoint di `RUNNING.md` è a mano; si chiuderebbe
con OpenAPI») è **superato** per la parte verificabile: l'elenco completo e verificato è ora
`docs/api/openapi.json`. `RUNNING.md` resta prosa per esseri umani.
