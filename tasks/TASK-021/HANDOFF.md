# TASK-021 → prossimo agente

- Una modifica di un DTO rende rosso `OpenApiContractTest`: è voluto. Rigenerare il file e i tipi del
  frontend nello stesso commit.
- springdoc non marca i campi di risposta come obbligatori: il frontend lo compensa in `types.ts`.
